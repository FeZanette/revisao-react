import styled from "styled-components";

export const HeaderContainer = styled.header`
    background-color: var(--deep-blue-secondary);
    height: 100px;
    text-align: center;
    font-size: 24px;

    /* tem que ser a mesma altura do container */
    line-height: 100px;
`